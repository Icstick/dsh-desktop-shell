schema_version: 1
id: MOD-LOCAL-TRANSPORT
name: "local-transport"
path: "crates/local-transport"
owner_role: runtime-and-security-owner
status: verified
milestone: M13
interfaces:
  - IF-NEGOTIATION
  - IF-INVOCATION
depends_on: []
security_review: passed
updated_at: "2026-09-13T00:45:00Z"
next_action: "M13（WI-M13）落地 peer-identity 载体族：loopback TCP（显式降级、内核对端身份不可得）、Windows named pipe、Unix domain socket（Linux SO_PEERCRED + /proc/<pid>/exe；macOS LOCAL_PEERPID + proc_pidpath；其他 Unix 明示 Unsupported 并 fail-closed）。carrier 抽象与对端身份见 ADR-0022；unsafe 仅限 Windows/macOS FFI 两处（crate 级 deny，逐处写明契约）。Windows 侧已合并 main（dca033b），Unix 侧随 WI-M13 推进。"
