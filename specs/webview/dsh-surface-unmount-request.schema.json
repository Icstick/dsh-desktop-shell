{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dsh-desktop.local/specs/webview/dsh-surface-unmount-request.schema.json",
  "title": "DshSurfaceUnmountRequest",
  "description": "Closes the fixed DSH Surface child without mutating the Managed runtime.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "environmentId", "expectedGeneration"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "environmentId": { "type": "string", "pattern": "^[a-z][a-z0-9-]{1,63}$" },
    "expectedGeneration": { "type": "integer", "minimum": 1, "maximum": 9007199254740991 }
  }
}
